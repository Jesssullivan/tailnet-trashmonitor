---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart trashcam units
  ansible.builtin.systemd:
    name: "trashcam@{{ item.id }}.service"
    state: restarted
  loop: "{{ trashcam_cameras }}"
